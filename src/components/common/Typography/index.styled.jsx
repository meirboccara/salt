import styled from "styled-components";

export const StyledTypographySpan = styled.span(({fontSize}) => ({
    color:"red",
    fontSize
}));

export const StyledTypographyDiv = styled.div(({fontSize}) => ({
    color:"blue",
    fontSize
}));